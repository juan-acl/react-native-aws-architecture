import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  continerText: {
    flex: 1,
    justifyContent: "center",
    alignItems: "center",
  },
  textNotFoundSearch: {
    color: "black",
    fontSize: 20,
    marginLeft: 17,
    marginTop: 10,
  },
});
